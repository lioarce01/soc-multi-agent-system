# MCP Servers Dependencies (Separate venv - ISOLATED from main app)
# Last updated: 2025-01-15
# Install in: mcp_servers/venv/

# ===== MCP Framework =====
fastmcp>=2.13.0
# FastMCP 2.13+ provides HTTP transport with host/port configuration

# ===== Base Utilities (minimal) =====
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.5.0,<3.0.0
aiohttp>=3.9.1,<4.0.0

# ===== Data Validation =====
email-validator>=2.1.0,<3.0.0

# ===== HTTP Server (for streamable_http transport) =====
uvicorn>=0.35.0,<0.36.0
# Used by FastMCP to run HTTP server

# ===== Memory Server Dependencies =====
# Required for memory_server.py (ChromaDB and embeddings)
# Prefer new packages (no deprecation warnings)
langchain-chroma>=0.1.0  # New Chroma integration (recommended)
langchain-huggingface>=0.1.0  # New HuggingFace embeddings (recommended)
# Fallback to deprecated packages if new ones not available
langchain-community>=0.4.0,<1.0.0
chromadb>=0.4.22
sentence-transformers>=2.3.1
langchain-core>=1.0.0,<2.0.0